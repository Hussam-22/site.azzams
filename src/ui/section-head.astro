---
const { title, description } = Astro.props;
---

<h2 class="text-5xl font-extrabold uppercase text-center sm:text-left">
  {title}
</h2>
{
  description && (
    <p class="text-lg mb-4 text-center sm:text-left">{`${description}`}</p>
  )
}
