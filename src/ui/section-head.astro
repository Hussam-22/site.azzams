---
const { title, description, gap } = Astro.props;
---

<h2
  class:list={[
    "text-5xl font-extrabold uppercase text-center sm:text-left",
    gap,
  ]}
>
  {title}
</h2>
{
  description && (
    <p class="text-lg mb-4 text-center sm:text-left">{`${description}`}</p>
  )
}
