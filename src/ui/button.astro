---
const { text, size } = Astro.props;

const buttonSize =
  size === "large" ? "text-xl py-3 px-6" : "text-base py-2 px-4";
---

<a
  class={`bg-gradient-to-br from-sky-500 to-violet-500 text-white ${buttonSize} rounded-4xl hover:from-sky-600 hover:to-violet-600 transition-colors duration-300 w-fit font-semibold`}
  href="/get-free-quote">{text}</a
>
